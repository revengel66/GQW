package com.example.gqw.analytics.support;

import java.util.LinkedHashMap;
import java.util.Map;

public final class SystemMetricReadingGuides {

    private static final Map<String, String> GUIDES = buildGuides();

    private SystemMetricReadingGuides() {
    }

    public static String guideFor(String metricCode) {
        return GUIDES.get(metricCode);
    }

    public static Map<String, String> guides() {
        return GUIDES;
    }

    private static Map<String, String> buildGuides() {
        Map<String, String> guides = new LinkedHashMap<>();
        guides.put("DB_QUERY_COUNT",
            guide(
                "Смотрите эту метрику только в одном и том же сценарии: тот же URL, тот же тип события и, по возможности, тот же этап (обычно DATABASE).",
                "Лучше брать короткие окна 15–30 минут и сравнивать их с таким же окном в другой день или до/после релиза.",
                "",
                "Рядом полезно смотреть размер ответа, количество элементов, длительность этапа и общее P95 запроса.",
                "",
                "Если AVG и P95 держатся около 1, поведение обычно стабильное. Если растут оба — запросов к базе стало больше, стоит искать лишние чтения. Если AVG почти тот же, а растёт только P95 — проблема появляется не всегда, а в отдельных тяжёлых случаях. Если MAX сильно выше P95 — есть редкие выбросы, их лучше разбирать по trace."
            )
        );
        guides.put("RESPONSE_SIZE_BYTES",
            guide(
                "Оценивайте размер ответа в одном и том же URL и в одной и той же операции, чтобы не смешивать разные сценарии.",
                "",
                "Сравнивайте его с количеством элементов, SQL-запросами, TTFB и временем рендера.",
                "",
                "Если размер растёт вместе с количеством элементов, это чаще всего нормально. Если размер вырос, а элементов столько же — в ответ, вероятно, попали лишние поля или дубли. Большой MAX при умеренном P95 говорит о редких тяжёлых ответах: их удобно искать через top значений."
            )
        );
        guides.put("RETRY_COUNT",
            guide(
                "Смотрите ретраи на одном endpoint и в сопоставимой нагрузке.",
                "",
                "Связывайте эту метрику с ERROR_CLASS, ERROR_CODE, NETWORK_ERROR и длительностью этапов.",
                "",
                "Если почти всегда ноль — контур стабильный. Если P95 растёт плавно — обычно это нестабильная зависимость или таймауты. Резкие всплески MAX чаще всего означают аварийные интервалы, которые нужно сопоставить с логами по времени."
            )
        );
        guides.put("ERROR_CODE",
            guide(
                "Смотрите коды ошибок в одном и том же URL и этапе, чтобы видеть повторяемый паттерн.",
                "",
                "Полезно смотреть рядом класс ошибки, HTTP-статус, сетевые ошибки и trace.",
                "",
                "Если один код явно доминирует, обычно это конкретный дефект в одной ветке логики. Если одновременно растут разные коды, это похоже на более общий инфраструктурный сбой. Короткий всплеск одного кода с быстрым восстановлением чаще означает разовый инцидент."
            )
        );
        guides.put("ERROR_CLASS",
            guide(
                "Сравнивайте долю классов ошибок в одинаковом окне времени.",
                "",
                "Рост VALIDATION обычно означает проблемы с вводом или контрактом. Рост BUSINESS — сбой в бизнес-правилах или предусловиях. Рост SYSTEM — техническая проблема, её обычно стоит разбирать в первую очередь."
            )
        );
        guides.put("ITEM_COUNT",
            guide(
                "Смотрите количество элементов на одном экране и с одинаковыми фильтрами.",
                "",
                "Сравнивайте с размером ответа, SQL-запросами и длительностью этапа.",
                "",
                "Если элементов становится больше и время растёт умеренно, это обычно нормальное масштабирование. Если элементов столько же, а время заметно растёт — узкое место в другом месте. Высокий MAX при низком AVG показывает редкие тяжёлые выборки."
            )
        );
        guides.put("PAYLOAD_SIZE_BYTES",
            guide(
                "Сравнивайте операции одного типа: write-операции лучше смотреть отдельно от GET.",
                "",
                "Если payload растёт вместе с длительностью API, часть задержки часто даёт сериализация и валидация. Если payload стабильный, а latency растёт — причина обычно в логике сервера или БД. Большой MAX при редких событиях — это единичные тяжёлые клиентские запросы."
            )
        );
        guides.put("VALIDATION_ERROR_COUNT",
            guide(
                "Анализируйте в связке с URL формы или метода и с типом навигации.",
                "",
                "Стабильно высокий уровень обычно говорит, что правила ввода неочевидны пользователю. Всплеск после релиза часто связан с изменением контракта или фронтовой валидации. Если здесь нули, а другие ошибки есть, причина не во входной валидации."
            )
        );
        guides.put("FRONTEND_TTFB_MS",
            guide(
                "Смотрите TTFB отдельно по URL и типу навигации.",
                "",
                "Высокий TTFB при нормальном DOM/LCP обычно указывает на сеть или серверный отклик. Если одновременно растёт API_DURATION, узкое место до передачи ответа. Если TTFB нормальный, а LCP плохой — тормозит уже клиентская часть."
            )
        );
        guides.put("FRONTEND_DOM_INTERACTIVE_MS",
            guide(
                "Сравнивайте только одинаковые страницы.",
                "",
                "Если DOM Interactive растёт при стабильном TTFB, обычно тяжелеет клиентский JS или DOM. Если метрика растёт вместе с размером передачи, страница в целом стала тяжелее."
            )
        );
        guides.put("FRONTEND_DOM_CONTENT_LOADED_MS",
            guide(
                "Смотрите на одинаковой странице и одинаковом типе навигации.",
                "",
                "Если TTFB низкий, а DOMContentLoaded высокий, обычно мешают блокирующие ресурсы в браузере. Если вместе растёт и LCP, страница стала тяжелее в целом."
            )
        );
        guides.put("FRONTEND_LOAD_EVENT_MS",
            guide(
                "Это метрика полного завершения загрузки страницы.",
                "",
                "Высокий Load Event при нормальном API обычно означает медленные картинки, скрипты или другую статику. Сильный хвост P95/P99 показывает, что у части пользователей ресурсы грузятся заметно тяжелее."
            )
        );
        guides.put("FRONTEND_TRANSFER_SIZE_BYTES",
            guide(
                "Сравнивайте размер передачи между версиями фронта на одном и том же URL.",
                "",
                "Если размер вырос без явного прироста функциональности, обычно утяжелился бандл или медиа. Если одновременно растёт LCP, объём уже заметно влияет на пользовательский опыт."
            )
        );
        guides.put("FRONTEND_LCP_MS",
            guide(
                "Это ключевой показатель того, как быстро пользователь видит основной контент.",
                "",
                "Полезно смотреть его вместе с TTFB, DOMContentLoaded и размером передачи.",
                "",
                "Высокий LCP при нормальном TTFB обычно указывает на клиентский рендер или ресурсы страницы. Если и LCP, и TTFB высокие, вклад есть и со стороны сервера, и со стороны доставки."
            )
        );
        guides.put("FRONTEND_INP_MS",
            guide(
                "Метрика показывает, насколько быстро интерфейс реагирует на действия пользователя.",
                "",
                "Если INP растёт без роста TTFB, чаще всего перегружен main thread или тяжёлые обработчики. Если проблема только на отдельных страницах, причина обычно локальна для конкретного UI-модуля."
            )
        );
        guides.put("FRONTEND_CLS_SCORE",
            guide(
                "Эта метрика показывает, насколько «прыгает» интерфейс при загрузке.",
                "",
                "Если CLS растёт, обычно не хватает заранее зарезервированного места под контент. Если значение стабильно около нуля, визуальная стабильность хорошая."
            )
        );
        guides.put("FRONTEND_API_DURATION_MS",
            guide(
                "Смотрите длительность вызова по API_URL и API_METHOD, чтобы сравнивать одинаковые запросы.",
                "",
                "Если клиентская длительность выше серверной, часть времени теряется в сети, канале или на клиенте. Если после релиза время выросло, а SQL-запросы не выросли, проблема вероятнее в сети или обработке данных, а не в БД."
            )
        );
        guides.put("FRONTEND_RENDER_AFTER_API_MS",
            guide(
                "Метрика показывает время от получения ответа API до видимого результата на экране.",
                "",
                "Высокие значения обычно говорят о тяжёлой обработке данных на клиенте. Если API работает стабильно, а эта метрика растёт, узкое место в UI-рендере."
            )
        );
        guides.put("FRONTEND_HTTP_STATUS",
            guide(
                "Разделяйте события по кодам ответа, чтобы быстро увидеть тип сбоя.",
                "",
                "Рост 4xx обычно связан с входными данными или контрактом клиента. Рост 5xx обычно означает серверные ошибки, их лучше разбирать по trace и классу ошибки."
            )
        );
        guides.put("FRONTEND_PAGE_URL",
            guide(
                "URL страницы — основной ключ сегментации фронтовых проблем.",
                "",
                "Если деградация только на одном URL, проблема обычно локальна для этого экрана. Если одинаковая деградация видна на многих URL, причина чаще системная."
            )
        );
        guides.put("FRONTEND_NAV_TYPE",
            guide(
                "Эта метрика задаёт контекст входа на страницу, поэтому сравнивайте показатели только внутри одного типа навигации.",
                "",
                "Если reload заметно хуже navigate, вероятны потери кешей или тяжёлые повторные загрузки. Если значения близки, путь загрузки стабилен для разных сценариев входа."
            )
        );
        guides.put("FRONTEND_API_URL",
            guide(
                "API URL помогает быстро выйти на конкретный backend endpoint.",
                "",
                "Если один API URL доминирует в хвосте latency, это главный кандидат на первичный разбор. Если деградируют многие URL одновременно, вероятнее общая инфраструктурная причина."
            )
        );
        guides.put("FRONTEND_API_METHOD",
            guide(
                "Разделяйте чтение (GET) и изменение данных (POST/PUT/DELETE), потому что риски у них разные.",
                "",
                "Если ошибки в основном на write-методах, выше риск потери или искажения операций. Если ошибки в основном на GET, страдает сценарий чтения данных пользователем."
            )
        );
        guides.put("FRONTEND_NETWORK_ERROR",
            guide(
                "Метрика отражает сетевые ошибки до входа в бизнес-логику сервера.",
                "",
                "Всплеск network error обычно связан с каналом, DNS, CORS или таймаутами. Если тут нули, а 5xx высокие, проблема уже на стороне сервера, а не сети."
            )
        );
        guides.put("FRONTEND_ERROR_MESSAGE",
            guide(
                "Смотрите повторяющиеся тексты, чтобы быстро собрать похожие ошибки в группы.",
                "",
                "Если часто повторяется один и тот же текст, это обычно конкретный воспроизводимый дефект. Если тексты очень разные в одном окне, это может быть широкая нестабильность или цепочка связанных проблем."
            )
        );
        guides.put("FRONTEND_TRACE_ID",
            guide(
                "Trace ID нужен для сквозного разбора цепочки фронт → backend → логи.",
                "",
                "Если один trace часто встречается в ошибках, вероятен повторяемый пользовательский сценарий. Если trace разные, но паттерн одинаков, причина обычно системная и находится на общем участке."
            )
        );
        guides.put("FRONTEND_CUSTOM_ATTRS_JSON",
            guide(
                "Дополнительные атрибуты показывают состояние интерфейса в момент события.",
                "",
                "Если ошибки связаны с конкретным состоянием, проблема обычно в отдельной UI-ветке. Если зависимости от атрибутов нет, причину стоит искать глубже — в общей логике или инфраструктуре."
            )
        );
        return Map.copyOf(guides);
    }

    private static String guide(String... lines) {
        return String.join("\n", lines);
    }
}
